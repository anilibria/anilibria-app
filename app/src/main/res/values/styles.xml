<resources>

    <!-- Base application theme. -->
    <style name="BaseAppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Base color settings-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- Чтобы не было видно статусбара -->
        <item name="colorPrimaryDark">@android:color/transparent</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="actionModeBackground">@color/colorPrimary</item>
        <item name="actionModeCloseButtonStyle">@style/actionModeCloseButtonStyle</item>
        <item name="actionOverflowButtonStyle">@style/OverFlow</item>
        <item name="alertDialogTheme">@style/AppTheme.Dialog</item>
        <item name="android:windowBackground">@color/windowBackground</item>

        <item name="toolbar_icon">@color/toolbar_icon</item>
    </style>

    <style name="AppTheme" parent="BaseAppTheme">
        <item name="android:windowTranslucentStatus">false</item>
    </style>

    <style name="PreferencesAppTheme" parent="BaseAppTheme">
        <item name="colorPrimary">@color/a_red</item>
        <item name="colorPrimaryDark">@color/a_red_dark</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="PlayerTheme" parent="AppTheme">
        <item name="colorPrimary">@color/playerColorPrimary</item>
        <item name="colorControlNormal">@color/white</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:singleLine">true</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="elevation">0dp</item>
    </style>

    <style name="AppTheme.Dialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:colorBackground">@color/cardBackground</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- OTHER -->
    <style name="OverFlow" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:tint">@color/toolbar_icon</item>
    </style>

    <style name="actionModeCloseButtonStyle" parent="Widget.AppCompat.Light.ActionButton.CloseMode">
        <item name="android:tint">@color/toolbar_icon</item>
    </style>

</resources>
